{
  "tickety_host": {
    "display_name": "Tickety API host",
    "description": "Public HTTPS hostname only, for example poc.tickety.example",
    "type": "text",
    "required": true,
    "secure": false,
    "regex": {
      "hostname": "^(?=.{1,253}$)(?!-)(?:[A-Za-z0-9-]{1,63}\\.)+[A-Za-z]{2,63}$"
    }
  },
  "binding_id": {
    "display_name": "Tickety binding ID",
    "description": "Binding ID created by the Tickety integration admin API",
    "type": "text",
    "required": true,
    "secure": false,
    "regex": {
      "uuid": "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$"
    }
  },
  "bootstrap_secret": {
    "display_name": "Embedded app bootstrap secret",
    "description": "Must match FRESHWORKS_APP_BOOTSTRAP_SECRET in the isolated Tickety deployment",
    "type": "text",
    "required": true,
    "secure": true
  }
}
