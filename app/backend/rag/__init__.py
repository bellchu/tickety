"""PostgreSQL/pgvector RAG v2 implementation.

The package is imported lazily by the v1 facade so deployments can keep the
additive schema and code installed while all v2 feature flags remain off.
"""
